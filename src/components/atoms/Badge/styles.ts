import { createStyles } from "@material-ui/core/styles";

const styles = createStyles({
  badge: {
    borderRadius: "2px",
    padding: "2px 4px",
  },
  text: {
    fontSize: "12px",
  },
});

export default styles;
