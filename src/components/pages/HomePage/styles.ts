import { createStyles } from '@material-ui/core/styles';

const styles = createStyles({
  loader: {
    height: '100%',
    width: '100%',
  },
  users: {
    margin: 'auto',
    maxWidth: '1130px',
    padding: '30px',
  },
});

export default styles;
