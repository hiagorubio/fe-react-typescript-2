import { createStyles } from '@material-ui/core/styles';

const styles = createStyles({
  count: {
    fontSize: '12px',
  },
  countValue: {
    fontSize: '12px',
    fontWeight: 600,
  },
  title: {
      fontSize: '24px',
  },
});

export default styles;
